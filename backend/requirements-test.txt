# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-test.txt

pytest==8.2.2
pytest-asyncio==0.23.8
httpx==0.27.0
httpx-ws==0.6.0        # WebSocket test client for ASGI apps
